function classPlot(model, X, t)
% Plot 2d classification data
%   X: 2xn data matrix
%   y: 1xn label

w = model.w;
w0 = model.w0;
figure;
spread(X,t);

y = w'X+w0;

hold on;
contour(X[1,x2,y,1);
hold off;